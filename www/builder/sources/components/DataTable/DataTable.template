{template .main}
<div class="@outer-container" onClick="handleClick">
	<ul>
	{foreach ~a as &i => &f while &i < 4}
		<li>{&f}</li>
	{/foreach}
	</ul>
	{switch .a($a, &s)}
		{case 1}
			111
		{/case}
	{/switch}
</div>
