{template .main}
<div class="@outer-container" onClick="handleClick">
	<ul>
	{foreach $a as &i => &f limit 2}
		<li>{&f}</li>
	{/foreach}
	</ul>
	{from &i = -100 to 3}
		<div>{&i}</div>
	{/from}
</div>
