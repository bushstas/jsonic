{template .main}
<div class="@outer-container" onCLick="handleClick" if="{$vvv==2}" else="34324{$a}32432{.handleChange($f)}">
	{foreach $a as &i => &f}
		<div>{&f}</div>
	{/foreach}

	<DataTableTabPanel onChange="handleChange"/>
	{$:aaa}
	<::hello>
		<Reason>
			<div>
				{$bbb}
			</div>			
		</Reason>
	</::hello>
</div>
