------------------------------

нельзя вызвать контроллер из класса если он не указан в initial controllers

------------------------------

initial interval = {
	'delay': 1000,
	'callback': this.onInterval
}

------------------------------

{for &a = 3 to &a.length step 6}

{/for}

------------------------------

{decline ~quantity 'item'}

------------------------------

{random 2}
	<div>1</div>
	<div>2</div>
	<div>3</div>
{/random}

------------------------------

обфускация названий реактивных пропсов

------------------------------

файлы .ext 
User.ext - доп методы для классов 

------------------------------

тип класса processor 

------------------------------

{$a:Corrector}

------------------------------

{.template .a as .b}  this.setState('b');

------------------------------

arg-className="frameless" в шаблоне Keywords не обфусцируется

------------------------------

неверный вывод ошибок в шаблонах include (не определен класс)

------------------------------

запилить геттеры и сеттеры

------------------------------

?~var = (!!~var?~var:'')
? ~var = (!!~var?' '+~var:'')
?~var:~b = (!!~var?~var:~b)

------------------------------

User.aaa()

------------------------------

Dictionary.cities

------------------------------

5*#data -> error
5*#data['aaa'] -> not error

------------------------------

$func(), ~func()

------------------------------

реализовать кастомные валидаторы

------------------------------

<component class="A" t="{%caption}">

<component class="A" t="{%main}"> нельзя передавать main / не может передавать сам себя

------------------------------

запилить JS константы

------------------------------

Ошибка вызова метода df класса DataTable из его шаблона. Метод не найден -> выводить код элемента

------------------------------

controller action 'before': Corrector, 'after': Corrector

-----------------------------



$items[0] = 1;
$items[1].name = 'Name';
$items.name[0] = 3;

$items.unset(0, 4, 7);
$items.remove('key');


$items.push(1);
$items.concat([1,3]);



$index++;
$index--;
$index += 4;
$index -= 4;
$index *= 4;
$index /= 4;

$isValid.toggle();


---------------------------------------

зарефакторить
ControllersParser.hasComponentMethod

------------------------------

обфускация __AC.favorites.get до __AC[0]

------------------------------

обфускация 'Component' до 'A'

------------------------------