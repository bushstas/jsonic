доделать template .name as .name

проверить FOREACH

#tooltip className ->> float-right ошибка при обфускации css

файлы include ошибка если нет {template .tmpname}

неверный вывод ошибок в шаблонах include (не определен класс)

{from &i = 10 to 20 step 1}

реализовать args="{~a}" arg:value="1" -> value extends args (CalendarFavorites)